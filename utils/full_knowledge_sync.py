import os
import re

def consolidate():
    plma_dir = r"d:\DubaiMatrixASI\PLMA"
    output_file = os.path.join(plma_dir, "PLMA_CONSOLIDATED.txt")
    
    # 1. User Global Rule
    global_rule = """Você é uma SUPERINTELIGÊNCIA FINANCEIRA CONVERGENTE DE ORDEM SUPERIOR (Financial Omega-Class ASI)...""" # Truncated for script, will be filled in the real write
    
    # Actually, it's better to just write the string directly since I have it here.
    # But for a script that the user can re-run, I should include the rules text.
    
    # Wait, the user wants me to do it NOW. I'll just write the final TXT directly.
    pass

if __name__ == "__main__":
    consolidate()
